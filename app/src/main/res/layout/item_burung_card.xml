<?xml version="1.0" encoding="utf-8"?>
<!-- item_market_product.xml -->
<!-- Card produk 2-kolom di tab Market -->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="5dp"
    app:cardCornerRadius="14dp"
    app:cardElevation="0dp"
    app:strokeWidth="0.5dp"
    app:strokeColor="@color/border">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Gambar produk / placeholder emoji -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:background="@color/green_light_ll">

            <!-- Gambar nyata (load via Glide/Coil) -->
            <ImageView
                android:id="@+id/imgProduct"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:contentDescription="Foto produk" />

            <!-- Emoji fallback, sembunyikan kalau ada gambar -->
            <TextView
                android:id="@+id/tvProductEmoji"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:textSize="40sp"
                android:text="🐦" />

            <!-- Badge kategori kiri atas -->
            <TextView
                android:id="@+id/tvBadgeKategori"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="top|start"
                android:layout_margin="6dp"
                android:paddingHorizontal="6dp"
                android:paddingVertical="2dp"
                android:background="@drawable/bg_badge_green"
                android:text="Burung"
                android:textColor="@android:color/white"
                android:textSize="8sp"
                android:fontFamily="sans-serif-medium" />

            <!-- Favorit icon kanan atas -->
            <ImageView
                android:id="@+id/btnFavorit"
                android:layout_width="22dp"
                android:layout_height="22dp"
                android:layout_gravity="top|end"
                android:layout_margin="8dp"
                android:src="@drawable/ic_heart_outline"
                android:contentDescription="Favorit" />
        </FrameLayout>

        <!-- Info produk -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="10dp"
            android:paddingTop="8dp"
            android:paddingBottom="8dp"
            android:gap="2dp">

            <TextView
                android:id="@+id/tvProductName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Murai Batu Medan"
                android:textSize="11sp"
                android:textColor="@color/text_dark"
                android:fontFamily="sans-serif-medium"
                android:maxLines="1"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/tvProductLokasi"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="📍 Medan, SU"
                android:textSize="9sp"
                android:textColor="@color/text_light" />

            <TextView
                android:id="@+id/tvProductHarga"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Rp 3.500.000"
                android:textSize="12sp"
                android:textColor="@color/green_dark"
                android:fontFamily="sans-serif-medium" />
        </LinearLayout>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>